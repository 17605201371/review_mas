# Accept-Side Evidence Formation Next Cut v1

## 下一刀建议

建议：`Evidence Context Selection v2 + Accept-Side Evidence Focus v1`。

理由：gold accept 的 evidence turns 普遍 broad，且 medium 支持主要来自 abstract；下一刀应先让 Evidence Agent 看到并抽取真实 method/result/table 片段，而不是把 abstract medium 直接升级为 strong。

## 不建议现在做

- 不调 accept/reject 阈值。
- 不把 payload-lineage support 直接接入 final decision。
- 不回到 sticky/throttle/gate 控制器。
- 不做 live state hygiene mutation。
